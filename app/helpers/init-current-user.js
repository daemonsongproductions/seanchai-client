/*
var initCurrentUser = function(currentUser) {
  log.info("current user: ", currentUser);
  if (currentUser != null ? currentUser.get('username') : void 0) {
    return var currentUser = currentUser;
  }
};

export default initCurrentUser;
*/
